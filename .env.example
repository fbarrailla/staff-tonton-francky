# Copy this file to .env.local and fill in your Supabase project credentials.
# If left empty, the app falls back to an in-memory mock backend (data persists in localStorage)
# so you can fully explore the interface without a backend.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
